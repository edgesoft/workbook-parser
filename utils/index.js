const regExps = require('./regexp')
module.exports = {
  guessType: require('./guessType'),
  regExps,
  sections: require('./sections'),
  getWorkbook: require('./getWorkbook'),
  cache: require('./cache'),
}
